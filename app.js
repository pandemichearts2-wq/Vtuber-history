const API_URL=window.GH_CONFIG?.API_URL||"";const state={profiles:[],videos:[],graduationMemories:[]};const $=id=>document.getElementById(id);function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}async function getData(){const u=new URL(API_URL);u.searchParams.set("action","publicData");const r=await fetch(u);return r.json()}async function postData(data){const r=await fetch(API_URL,{method:"POST",headers:{"Content-Type":"text/plain;charset=utf-8"},body:JSON.stringify(data)});return r.json()}function renderVideos(a){$("videoSlider").innerHTML=a.length?a.slice(0,3).map(v=>`<article class="video-card"><h3>${esc(v.title||v.activityName||"思い出の動画")}</h3><p>${esc(v.activityName||"")}</p><a href="${esc(v.url)}" target="_blank" rel="noopener">動画を見る</a></article>`).join(""):'<p class="muted">公開中の動画はまだありません。</p>'}function renderProfiles(a){$("profileList").innerHTML=a.length?a.map(v=>`<article class="profile-card"><h3>${esc(v.activityName)}</h3><p>${esc(v.reading||"")}</p><p>${esc(v.affiliation||"")}</p></article>`).join(""):'<p class="muted">該当するプロフィールはありません。</p>'}function showMemory(i=0){const a=state.graduationMemories,s=$("graduationMemory");if(!a.length){s.classList.add("hidden");return}const v=a[i%a.length];s.classList.remove("hidden");$("graduationTitle").textContent=`${v.yearsAgo?`${v.yearsAgo}年前の今日、`:"今日は"}${v.activityName}さんが卒業しました`;$("graduationText").textContent="あの日の配信や思い出を、もう一度振り返ってみませんか。";const b=$("graduationVideoBtn");v.videoUrl?(b.href=v.videoUrl,b.classList.remove("hidden")):b.classList.add("hidden");$("graduationNextBtn").dataset.index=(i+1)%a.length}async function init(){try{const d=await getData();state.profiles=d.profiles||[];state.videos=d.videos||[];state.graduationMemories=d.graduationMemories||[];renderVideos(state.videos);renderProfiles(state.profiles);showMemory()}catch(e){$("videoSlider").innerHTML='<p class="muted">データを取得できませんでした。</p>'}}$("randomVideoBtn").onclick=()=>{if(state.videos.length){const v=state.videos[Math.floor(Math.random()*state.videos.length)];window.open(v.url,"_blank","noopener")}};$("graduationNextBtn").onclick=e=>showMemory(Number(e.currentTarget.dataset.index||0));$("searchBtn").onclick=()=>{const q=$("searchInput").value.toLowerCase().trim();renderProfiles(!q?state.profiles:state.profiles.filter(v=>[v.activityName,v.reading,v.affiliation,v.nickname,v.streamStyle].some(x=>String(x||"").toLowerCase().includes(q))))};document.querySelectorAll(".tab").forEach(b=>b.onclick=()=>{document.querySelectorAll(".tab").forEach(x=>x.classList.remove("active"));b.classList.add("active");$("formType").value=b.dataset.form});$("registrationForm").onsubmit=async e=>{e.preventDefault();const btn=e.currentTarget.querySelector('[type="submit"]'),msg=$("formMessage");btn.disabled=true;btn.textContent="送信中…";msg.textContent="";const data=Object.fromEntries(new FormData(e.currentTarget).entries());data.action="submit";data.author="匿名ユーザー";try{const r=await postData(data);if(!r.ok)throw Error(r.message||"送信できませんでした");msg.textContent="送信しました。管理者の確認後に反映されます。";e.currentTarget.reset();$("formType").value=document.querySelector(".tab.active").dataset.form}catch(x){msg.textContent=x.message}finally{btn.disabled=false;btn.textContent="匿名ユーザーとして送信"}};const audio=$("bgmAudio"),toggle=$("bgmToggle");audio.volume=.28;function ub(){toggle.textContent=audio.paused?"BGMを再生":"BGMを停止"}toggle.onclick=async()=>{audio.paused?await audio.play():audio.pause();ub()};audio.play().catch(()=>{});document.addEventListener("pointerdown",()=>{if(audio.paused)audio.play().catch(()=>{})},{once:true});audio.onplay=audio.onpause=ub;ub();init();